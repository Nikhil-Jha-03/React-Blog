import React from 'react'

const CommentComponent = () => {

  return (
    <div>CommentComponent</div>
  )
}

export default React.memo(CommentComponent);

// take single object and make according to it